from django.apps import AppConfig


class DocumentsConfig(AppConfig):
    """Document filing, categories, templates, generation, secure links."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "documents"
