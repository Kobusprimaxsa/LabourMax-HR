from django.apps import AppConfig


class DisciplineConfig(AppConfig):
    """Disciplinary cases, actions, evidence."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "discipline"
