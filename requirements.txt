# Django 5.2.8 is the first release that supports Python 3.14 - do not go below it.
Django>=5.2.8,<6.0

psycopg[binary]>=3.2
djangorestframework>=3.15
django-environ>=0.11
celery>=5.4
redis>=5.2
cryptography>=44
argon2-cffi>=23
python-dateutil>=2.9
openpyxl>=3.1

# PDF generation lives in requirements-pdf.txt - it needs native GTK libraries on
# Windows and is not required until phase P8. Do not add it here.
